<?xml version="1.0" encoding="UTF-8"?>
   <!DOCTYPE mapper
             PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
             "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
             
   <mapper namespace="spring.project.tcat.persistence.TSGuestDAO">
   
   <select id="idCheck" resultType="int">
	select count(*) from member where id=#{login_id}
	</select>
   
   <!-- host구분 -->
	<select id="HostCnt" parameterType="java.util.Map" resultType="int">
	select host from mvc_member where id=#{login_id}
	</select>
	
	<!-- 회원 정보 수정 -->
	<update id="updateMember" parameterType="spring.project.tcat.VO.MemberVO">
		update member set member_pwd=#{member_pwd}, member_name=#{member_name}, member_hp=#{member_hp}, member_email=#{member_email} where member_id=#{id}
	</update>
   </mapper>